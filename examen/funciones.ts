/* 1. Crear una funcion llamada preparar sopa que tendra de parametros
nombre de la sopa, cantidad de platos, temperatura, el valor de retorno
sera los minutos en el preparado de platos */
function preparaSopa(
  nombre: string,
  cantidad: number,
  temperatura: number,
): number {
  return 232;
}

/* 2. Crear una funcion llamada salirDeClases, tendra de parametros
hora de salida, precio de de pasaje, direccion de casa, el valor de retorno
sera un boolean */
function salirDeClases(
  horaSalida: string,
  precio: number,
  direccion: string,
): boolean {
  return false;
}

/* Crear una funcion llamada comprar moto, tendra de parametros
marca de la moto, precio, modelo, fecha de compra, el valor de
retorno sera el monto total de la compra */
function comprarMoto(
  marca: string,
  precio: number,
  modelo: string,
  fecha: string,
): number {
  return 121312;
}

/* Crear una funcion llamada destruir edificio
los parametros seran nombre del edificio, cantidad de bombas,
fecha, el valor de retorno sera un boolean*/
function destruirEdificio(
  nombreEdificio: string,
  cantidadBombas: number,
  fecha: string,
): boolean {
  return false;
}

/* Cear una funcion llamada ir al colegio, los parametros seran
nombre del colegio, hora de salida, monto del pasaje,
el valor de retorno sera un boolean */
function irAlColegio(
  nombre: string,
  horaSalida: string,
  montoPasaje: number,
): boolean {
  return false;
}

/* Crear una funcion que se llame solicitarPedido, los parametros seran
nombre del producto, precio, hora del pedido, el valor de retorno
sera el monto total */
function solicitarPedido(
  producto: string,
  precio: number,
  horaPedido: string,
): number {
  return 10;
}

/* Crear una funcion que se llame guardar archivo
tendra de parametros el nombre de archivo, tamaño, fecha
y el valor de retorno sera un boolean */
function guardarArchivo(
  nombreArchivo: string,
  tamanio: number,
  fecha: string,
): boolean {
  return false;
}
