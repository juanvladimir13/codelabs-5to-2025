/* Escribir una funcion llamada comprarAlmuerzo
de parametros tendra, nombre de la sopa, nombre del segundo
nombre de refrezco y el valor de retorno sera el monto pagado */
function comprarAlmuerzo(
  sopa: string,
  segundo: string,
  refrezco: string,
): number {
  return 0;
}

/* Escribir una funcion llamada venderProducto los parametros seran
 en producto, precio, estaVencido y  el valor de retorno sera un boolean */
function venderProducto(
  producto: string,
  precio: number,
  estaVencido: boolean,
): boolean {
  return true;
}

/* Escribir una funcion que se llame construir edificio
de parametros tendra la altura, largo del edificio, nombre y estadoFinalizado */
function construirEdificio(
  altura: number,
  largo: number,
  nombre: string,
  estaFinalizado: boolean,
): void {
}
