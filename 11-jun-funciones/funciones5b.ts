/* Escribir una funcion llamada pedir cafe, los parametros seran
tipo de cafe, precio, el tipo de retorno sera el precio del pedido */
function pedirCafe(tipoCafe: string, precio: number): number {
  return 50;
}

/* Escribir una funcion llamada calcularPeso los parametros seran
genero, edad, nombre, el valor de retorno sera un texto */
// M = false,  F = true  edad: 5 meses, 1 anio y 5 meses
function calcularPeso(genero: boolean, edad: string, nombres: string): string {
  return "2 kilos 500 g";
}

/* Escribir una funcion llamada solicitar licencia, los valores seran
estudiante, motivo, fecha, el valor de retorno sera boolean */
function solicitarLicencia(
  estudiante: string,
  motivo: string,
  fecha: string,
): boolean {
  return true;
}

/* Escribir una funcion sin valor de retorno y con 1 parametro de tipo texto
y un parametro de tipo boolean */
function sinNombreLaFuncion(texto: string, genero: boolean): void {
}
