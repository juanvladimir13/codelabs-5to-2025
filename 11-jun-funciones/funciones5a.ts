/* Escribir una funcionar llamada pedirPollo
los parametros seran, tipodeCoccion, precio el valor de retorno
sera en precio del pedido
*/
function pedirPollo(tipodeCoccion: string, precio: number): number {
  return 13;
}

/* Escribir una funcion llamada construir casa, los datos que
tendra seran, ubicacion, monto total, metros cuadrados,
estadoFinalizado, el valor de retorno sera un boolean
 */
function construirCasa(
  ubicacion: string,
  montoTotal: number,
  metrosCuadrados: number,
  estadoFinalizado: boolean,
): boolean {
  return true;
}

/* Escribir una funcion llamada irAlColegio, tendra los valores de
dia, hora, montoRecreo, voyTarde
*/
function irAlColegio(
  dia: string,
  hora: string,
  montoRecreo: number,
  voyTarde: boolean,
): void {
}

function holaMundo(): number {
  return 0;
}
